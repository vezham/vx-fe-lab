import { Meta } from '@storybook/react'

import { A } from '@vezham/storybook'

export default {
  title: 'Hooks/useString',
  tags: ['!autodocs'],
  component: A
} as Meta<typeof A>

export const Info = {
  args: {
    label: 'useString'
  }
}
